/**
 * Custom-event name used to sync the Membership card selection with
 * the Contact form's plan dropdown. The event `detail` is the plan id
 * ("monthly" | "quarterly" | "yearly"), which matches the form schema.
 */
export const SELECT_PLAN_EVENT = "obee:selectPlan";
